name: Momentum Detector

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Comprobar funcionamiento
        run: echo "🚨 MOMENTUM DETECTOR funcionando correctamente"
